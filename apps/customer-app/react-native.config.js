const path = require('path');

module.exports = {
  dependencies: {
    'react-native-linear-gradient': {
      root: path.resolve(__dirname, '../../node_modules/react-native-linear-gradient'),
    },
  },
};
